# 🎬 AI Video Studio Pro

## ⚡ البداية في 60 ثانية

### الطريقة 1: التشغيل السريع
1. انقر نقراً مزدوجاً على `setup.bat`
2. بعد التثبيت، انقر على `run.bat`
3. افتح المتصفح على: `http://localhost:8501`

### الطريقة 2: عبر VS Code
1. افتح مجلد المشروع في VS Code
2. افتح Terminal: **Ctrl + `**
3. اكتب: `streamlit run app.py`

## ✨ المميزات الرئيسية

### 🎥 المونتاج الذكي
- معالجة فيديو تلقائية بالذكاء الاصطناعي
- مكتبة مؤثرات بصرية غير محدودة
- تحسين ألوان وتثبيت تلقائي
- قص ذكي للمقاطع

### 🔊 المكتبة الصوتية
- 500+ مؤثر صوتي مجاني
- موسيقى خلفية احترافية
- همهمات بشرية واقعية
- مؤثرات انتقال وتشويق

### 🌐 واجهة الويب
- موقع ويب متكامل بدون حدود
- واجهة عربية احترافية
- رفع وتحميل مباشر
- معاينة فورية


## 🖥️ تشغيل البرنامج كأبلكيشن سطح مكتب

1. بعد التثبيت، شغّل الملف:
	```
	python run_app.py
	```
	سيتم فتح التطبيق تلقائياً في المتصفح.

### تحويل التطبيق إلى ملف EXE (تشغيل مباشر)

1. ثبّت PyInstaller:
	```
	pip install pyinstaller
	```
2. أنشئ الملف التنفيذي:
	```
	pyinstaller --onefile --noconsole run_app.py
	```
3. ستجد الملف التنفيذي في مجلد dist/run_app.exe
4. شغّل run_app.exe وسيتم فتح التطبيق تلقائياً.

> **ملاحظة:** يجب أن تكون جميع الملفات (app.py، المجلدات، إلخ) في نفس المجلد مع run_app.exe أو استخدم --add-data مع PyInstaller لنقل الملفات.

---

## 🛠️ إصلاح المشاكل الشائعة

### إذا لم يعمل Python:
```bash
setup.bat
```
